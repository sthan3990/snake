// Used by client.js
const ipAddress = `localhost`;
const ipPort = `8080`;
const userName = `SHAN`;
const randomMessage = `blah snakesss`;

module.exports = {
  ipAddress,
  ipPort,
  userName,
  randomMessage,
};